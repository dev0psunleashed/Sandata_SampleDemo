SELECT * FROM (SELECT ROWNUM ROW_NUMBER, COUNT(*) OVER() TOTAL_ROWS, R1.* FROM (

  SELECT * FROM (

      (SELECT *
          FROM HCPCS_LKUP
          %s
          ORDER BY %s %s)
  )

) R1)

WHERE ROW_NUMBER BETWEEN %d AND %d
